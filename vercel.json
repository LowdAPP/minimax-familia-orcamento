{
  "buildCommand": "cd familia-financas && npm install && npm run build",
  "outputDirectory": "familia-financas/dist",
  "installCommand": "cd familia-financas && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

