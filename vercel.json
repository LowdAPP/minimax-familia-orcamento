{
  "buildCommand": "cd familia-financas && pnpm install && pnpm build",
  "outputDirectory": "familia-financas/dist",
  "installCommand": "cd familia-financas && pnpm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

